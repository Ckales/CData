[package]
name = "cdata_rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
cdata-core = { version = "0.1.0", path = "../../crates/cdata-core" }
flutter_rust_bridge = "=2.13.0"
tokio = { version = "1.53.1", features = ["rt-multi-thread"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }
